// const fs = require("fs");
// let input = fs.readFileSync("./dev/stdin").toString().trim().split("\n");

// let n = input.shift().split("").map((v) => Number(v));
// let inorder = input.shift().split(" ").map((v) => Number(v));
// let preorder = input.shift().split(" ").map((v) => Number(v)).reverse();
// let root=0;
// let answer = []; //출력할 임의의 배열

// function DFS(L) {
//   for (let i=0; i<n; i++) {
//     root = preorder[n-1]
//     for (let i = 0; i < n; i++) {
//       if (inorder[i] === root) {
//         //인오더에서 루트가 같으면 그 인덱스 기준 왼쪽이 왼쪽 트리
//         for (let i = 0; i < n; i++) {
//           if (preorder[i] === 5) {
//           }
//         }
//       }
//     }
//   }
// }
// DFS(0);

// console.log(root);